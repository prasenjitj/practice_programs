function spinalCase(str) {
    str = str.split('');
    return str.forEach(function(x) {
            if (x.match(/[A-Z]/) !== -1) {}
        }
    }
    spinalCase("The_Andy_Griffith_Show");
    spinalCase("AllThe-small Things");
